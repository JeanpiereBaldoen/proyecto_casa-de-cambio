/**\n * Servicio de recuperación de contraseña\n * API: POST /api/auth/recuperar-password, POST /api/auth/enviar-codigo\n */\n\nimport { RecuperarPasswordFormInterface } from '../interfaces/RecuperarPasswordForm';\n\nexport const recuperarService = {\n  /**\n   * Envía un código de verificación al email del usuario\n   * TODO: Implementar POST a /api/auth/enviar-codigo\n   */\n  async enviarCodigo(email: string): Promise<void> {\n    await new Promise((resolve) => setTimeout(resolve, 500));\n    console.log('[recuperarService.enviarCodigo] Enviando código a:', email);\n    // TODO: Reemplazar con llamada real a API\n  },\n\n  /**\n   * Recupera la contraseña verificando el código y creando una nueva\n   * TODO: Implementar POST a /api/auth/recuperar-password\n   */\n  async recuperarPassword(data: RecuperarPasswordFormInterface): Promise<void> {\n    await new Promise((resolve) => setTimeout(resolve, 500));\n    console.log('[recuperarService.recuperarPassword] Recuperando contraseña para:', data.email);\n    // TODO: Reemplazar con llamada real a API\n  },\n};\n